import {LoginForm} from "../components/login-form/LoginForm.tsx";

export const LoginPage = () => {

    return (
        <section className='w-[200px] mx-auto py-8'>
            <LoginForm/>
        </section>
    );
};